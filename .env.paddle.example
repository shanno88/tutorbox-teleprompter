# ===========================================
# Paddle Billing 配置示例
# ===========================================
# 复制此文件为 .env，并填入你的实际值
# 
# 注意：
# - 前端变量必须以 VITE_ 开头（Vite 要求）
# - 后端变量不需要 VITE_ 前缀
# ===========================================

# ---------- 前端配置 (Vite) ----------
# 客户端 Token（在 Paddle Dashboard > Developer Tools > Client-side tokens 获取）
# 注意：这是 Billing 的 client-token，不是旧的 Public Key
VITE_PADDLE_CLIENT_TOKEN=your_client_token_here

# 环境：sandbox（测试）或 production（正式）
# 开发环境用 sandbox，生产环境用 production
VITE_PADDLE_ENVIRONMENT=sandbox

# DeepSeek API Key（用于 AI 功能）
# 在 DeepSeek 平台 https://platform.deepseek.com/ 获取
VITE_DEEPSEEK_API_KEY=your_deepseek_api_key_here

# ---------- 后端配置 (Express) ----------
# Paddle Seller ID（商户 ID）
PADDLE_SELLER_ID=your_seller_id_here

# Paddle API Key（服务端 API 密钥）
# 在 Paddle Dashboard > Developer Tools > Authentication 获取
PADDLE_API_KEY=your_api_key_here

# Webhook 签名密钥（用于验证 webhook 回调）
PADDLE_WEBHOOK_SECRET=your_webhook_secret_here

# ---------- 价格 ID (Price IDs) ----------
# 重要：Paddle Billing 使用 Price ID，不是 Product ID
# 在 Paddle Dashboard > Catalog > Prices 获取
# 
# 月度订阅价格 ID
PADDLE_PRICE_ID_MONTHLY=pri_monthly_price_id_here

# 年度订阅价格 ID
PADDLE_PRICE_ID_YEARLY=pri_yearly_price_id_here

# 兼容旧命名（可选，推荐使用上面的 PADDLE_PRICE_ID_*）
# PADDLE_PRODUCT_ID_MONTHLY=pri_monthly_price_id_here
# PADDLE_PRODUCT_ID_YEARLY=pri_yearly_price_id_here

# ---------- 环境设置 ----------
NODE_ENV=development

# ===========================================
# 如何获取这些值：
# ===========================================
# 1. 登录 Paddle Dashboard: https://vendors.paddle.com/
# 2. Seller ID: Dashboard 右上角或 Settings > Company
# 3. Client Token: Developer Tools > Client-side tokens > Generate token
# 4. API Key: Developer Tools > Authentication > Generate API key
# 5. Price ID: Catalog > 选择产品 > Prices 标签页 > 复制 Price ID (pri_xxx)
# 6. Webhook Secret: Developer Tools > Notifications > 创建 webhook 时获得
